# Astrbot All in One Plugin

只是一个供作者用的插件

## bot嘴替 /sendto 

用于让bot给另一个用户（群）发消息，语法：`/sendto uid prompt`，其中 uid 如果是群聊，群id前必须加上 `g:`，如果是用户，用户uid前可以加上`f:`。

## 保持群成员的正常性取向（） 

当群聊中的消息出现“男朋友”等字样时，会提醒群友不要做男同（某个红太阳除外）。

## cpc比赛查询 /cpcquery

用于查询最近的CodeForces和Atcoder的比赛。